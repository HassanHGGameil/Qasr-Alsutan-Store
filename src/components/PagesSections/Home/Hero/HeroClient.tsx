// HeroClient.tsx
import React from 'react';
// import HeroCard from './HeroCard';
// import HeroData from './heroData';
// import { THero } from './heroType';

const HeroClient = () => {
  // const heroData: THero = HeroData;

  return (
    <section id='hero' className="hero-section">
      {/* <HeroCard {...heroData} /> */}
    </section>
  );
};

export default HeroClient;